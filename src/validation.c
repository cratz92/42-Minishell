#include "../include/minishell.h"

/*
NOTES sudo code, everything welcomed

*/


void	ft_validations(t_minishell **shell)
{
	/* sudo code
	1. check if CMD exist
	2. ARGS ...
	3. if not- return exit code
	4. What else?




	if $> billy=joe ls
	output> ls (var assignation is ignored);

	if $> henry=joe dsfasdfsa
	output> dsfasdfsa (var assignation is ignored);

	*/
}